-- ========================================
-- SEED DATA FOR LOCAL DEVELOPMENT
-- ========================================

-- This file contains sample data for local development
-- It will be loaded automatically when running `supabase db reset`

-- Additional sample data can be added here if needed
-- The main sample data is already included in the migration file

-- Example: Create a sample drop for testing
-- INSERT INTO drops (date, location_id, status) 
-- SELECT CURRENT_DATE + INTERVAL '1 day', id, 'upcoming' 
-- FROM locations 
-- WHERE name = 'Impact Hub Lisbon' 
-- LIMIT 1;
